# CI 分片改造实测

2026-09-26 的[完整资格验证 36227491727](https://github.com/xyqyear/mc-admin/actions/runs/36227491727)成功，验证源码为 `76903e1ca1215aafe54aa5cbe153f23e0292368f`。手动运行设置 `publish=false`，所有测试和产物身份检查通过，未发布镜像或修改版本标签。

对照为[成功基线 36148282102](https://github.com/xyqyear/mc-admin/actions/runs/36148282102)，源码 `45a47640dedb60948b82418d5c1f989377e91af4`。基线包含发布步骤，因此下表比较从 workflow 创建到 qualification 完成的同一范围，排除发布耗时。

## 墙钟时间

| 范围 | 改造前 | 改造后 | 变化 |
| --- | ---: | ---: | ---: |
| 创建至完整资格检查通过 | 55:18 | 10:00 | 缩短 81.9% |
| 后端 discover 至审计完成 | 55:01 | 8:28 | 缩短 84.6% |
| API 最慢分片的测试步骤 | 11:11 | 6:34 | 缩短 41.3% |
| API 最慢分片的整个 job | 12:12 | 7:11 | 缩短 41.1% |
| 浏览器测试步骤 | 3:59 | 4:25 | 增加 26 秒 |
| 浏览器整个 job | 5:15 | 5:37 | 增加 22 秒 |

一次成功样本不代表稳定分位数。测试数据增加、runner 和网络波动、准备成本都会影响时间。浏览器增加了可确定触发的地图状态延迟回归，其完整断言保留；并非所有环节都变快了。

## 后端分片

| 分片 | 测试数 | 测试步骤 | 整个 job |
| --- | ---: | ---: | ---: |
| 1 | 29 | 6:58 | 7:29 |
| 2 | 729 | 6:01 | 6:48 |
| 3 | 734 | 6:06 | 6:37 |
| 4 | 732 | 5:17 | 5:37 |

四片均在 UTC 07:40:56 开始。数量不同是按历史执行成本分配的结果：第一片保留最慢的世界恢复文件，其他文件分配至剩余三片。每个 runner 内仍顺序运行 pytest，未引入共享 Docker 主机上的并行测试进程。

本轮 **2,224 项测试全部通过**，较基线 2,183 项增加了基础设施回归覆盖。独立下载复核确认：完整 inventory 与计划一致，所有 node ID 恰好执行一次；6,672 条 setup/call/teardown 记录全部成功；JUnit 与逐测试 CSV 数量、身份和阶段时间一致，没有跳过或失败。

四份原始覆盖率重新合并后，与 CI 的全部 306 个文件逐行命中一致：覆盖 18,656 条可执行行中的 16,362 行，行覆盖率 **87.7%**。各片保留覆盖率采集，只将报告生成集中到审计 job。

## API、浏览器和单元测试

| API 分片 | 改造前测试步骤 | 改造后测试步骤 | 改造后整个 job | 用例数 |
| --- | ---: | ---: | ---: | ---: |
| 1 | 11:11 | 5:20 | 6:11 | 18 |
| 2 | 8:33 | 6:24 | 6:58 | 33 |
| 3 | 3:04 | 6:34 | 7:11 | 30 |

API **81/81** 用例通过，完整 catalog 无遗漏或重复，**77/77** 自有环境清理完成；159 个 API operation 都有通过用例中的成功调用记录。这表示调用观测完整，不等同于所有行为分支都已覆盖。每片仍是两个 worker、一个 Minecraft 槽位，复用组保持完整。

浏览器 **6/6** 场景通过，没有跳过、重试通过或 flaky，**1/1** 自有环境清理完成。中断恢复场景在地图状态返回前打开历史抽屉，返回后继续检查同一恢复记录，再执行真实回滚和文件字节校验。

前端 Vitest **144/144**，runner 耗时 22.41 秒；架构检查 **4/4**。Go JSON 包含 **105 条通过事件**，其中有父测试与子测试，覆盖 50 个顶层测试函数、11 个包；不能把嵌套事件视为 105 个独立用例或相加其耗时。

## 调度与证据

本次完整运行观测到最多八个成功 job 同时占用 runner：四个后端分片、三个 API 分片和浏览器。基线的主要限制是后端矩阵明确设置的串行执行；这些记录不能证明账号的并行额度上限。其他仓库工作流仍可能竞争 runner。

所有成功 job 的累计运行时间（排除发布）从 84:29 降至 57:57。该值不是墙钟时间或精确计费分钟，也不包含开发过程中的失败尝试。

完整运行提供以下可下载 artifacts，保留期以 workflow 配置为准：

- `backend-test-reports`：逐测试 `test-durations.csv`、阶段 JSON、JUnit、原始及合并覆盖率。
- `backend-test-inventory`：完整清单、分片计划、策略与历史权重身份。
- `e2e-results-1/2/3`：每项 API 用例的执行、队列等待、资源等待、准备、断言、清理和环境销毁时间。
- `browser-results`、`frontend-test-results`、`go-test-results`：浏览器、Vitest 和 Go 的逐项结果与时间。
- 资格证明及候选产物：源码、镜像身份和各测试门禁的一致性证据。

首轮验证曾发现 ShellCheck 重定向问题和一个真实界面缺陷：地图初始化切换侧栏结构，导致已打开的恢复历史抽屉卸载。两者均已修复并通过第二轮完整 CI；未通过放宽断言、延长超时或跳过场景来获得成功。

目前剩余长尾是单个世界恢复文件及部分 Minecraft 场景。后续应积累多次成功运行的精确计时，再审查文件拆分或更新历史权重。新测试始终由完整清单发现，历史权重只影响分配位置，不控制测试是否执行。E2E 调度器变更另需按[运行说明](ci.md)执行禁用复用的隔离验证。
